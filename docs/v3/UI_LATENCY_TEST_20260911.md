# 真实会话页面连续生图测试（2026-09-11）

本次用桌面入口启动正式前后端，使用用户运行数据库、Windows 已保存凭据与真实云显卡。浏览器中创建独立测试会话，通过 LLM 编译、补充负向建议、选择目标和保存设置，再逐张点击“生成图片”；没有用脚本/API 代替生成按钮。后台脚本只读观察 generation_runs。

连接恢复：中断后的浏览器会话已丢失，重新启动本次测试服务；当前 117.50.193.201:23 的指纹与此前验证的同一主机一致，通过设置页面确认保存并完成连接测试（RTX 3080 Ti、队列 0）。

共同条件：768×1024、每次 1 张、随机种子（六次不同，避免重复工作流缓存）。美学 v1.1 为 35 步、CFG 4.5、Euler/normal；Turbo v1.1 为 10 步、CFG 1、er_sde/simple。正向为成年女性、蓝色外套白衬衫、捧花、傍晚旧街道。保留可见负向建议；Turbo CFG 1 不作常规负向引导效果比较。

| 次序 | 模型 | 点击至观察到云端运行 | 点击至下载完成 |
| --- | --- | ---: | ---: |
| 1 | Aesthetic v1.1 | 3.50 s | 21.18 s |
| 2 | Aesthetic v1.1 | 3.64 s | 21.19 s |
| 3 | Aesthetic v1.1 | 4.01 s | 21.71 s |
| 4 | 切换至 Turbo v1.1 | 3.90 s | 40.71 s |
| 5 | Turbo v1.1 | 3.17 s | 10.16 s |
| 6 | Turbo v1.1 | 3.23 s | 10.03 s |

计时口径：点击前记录浏览器工具端 Date.now，下载完成取后端 completed_at；云端运行状态由每 0.2 秒一次的只读数据库观察记录，包含轮询误差，不代表精确 GPU kernel 开始时刻。图片呈现通过真实页面结果卡与 DOM naturalWidth > 0、complete=true 核验；没有高频采集渲染时刻，故上表不是“精确到图片呈现”的耗时。六张均确认完成加载。页面观察之间的工具操作间隔不计作软件延迟。

结果：此次六张没有复现“连续生成导致提交前等待持续增加”。接收前目标解析 0.20–0.41 秒；执行器 SSH/环境/节点校验/提交合计 1.80–2.20 秒。Turbo 切换后首张额外约 30 秒发生在云端运行阶段，与模型加载开销相符，但本次没有独立采集模型加载日志，不能据此确定具体节点或排除偶发问题。首次美学生成使用已运行过的云环境，不是完全冷启动测试。

实测发现并修复：正常提交期间也显示“上次生成请求的接受结果尚未确认”，误导用户以为请求异常。现在只在没有正在处理的操作、仍有未确认请求时显示恢复查询区，正常提交仍显示提交进度。会话页 11 项测试通过，包含丢失回执后的同幂等键恢复；TypeScript 检查和生产构建通过。修复在本轮生成之后构建，六张运行页面未刷新，不能作为修复文案的浏览器回归证据。

原始只读轨迹与聚合数据：`.local/ui-latency-20260911/trace.jsonl`、`results.json`。所有测试图保存在正常生成输出与画廊中。本次未提交或推送代码。
