# Qwen3.7-Max 五场景重复验证（2026-09-10）

结论：固定同一改写 SYSTEM，五个场景各三轮、独立三组，共 45 次 rewrite 全部完成并恢复一致。逐轮复核，改写保留了传入的核心要求，没有发现主体数量、左右关系、锁层、未声明画师或资源回流错误。**整条参考流程尚未通过**：三次 prompt ingest 有一次漏掉原始提示词中的 monochrome，随后三轮继承了这份不完整要求。不能包装成 45 次全流程质量通过。

## 调用与复现

OpenCode Go 请求模型 qwen3.7-max，temperature=0.7、top_p=0.9、max_tokens=4096、高级参数启用、按注册参数请求关闭思考；未改保存的默认配置。各组有独立工作台、相同场景和 SYSTEM SHA256：

`1e063e27325634cfa75baa0b9e995afb83b56d1898d8553e8d17c2ef95cb7cd2`

`.local/conversation-acceptance/qwen37-45-summary.json` 指向八份原始报告。本轮增加 36 次 rewrite 和 3 次 prompt ingest；此前初筛三组 9 次 rewrite 使用相同规则和场景，纳入总计 45 次。未删除失败候选的历史报告，没有自动重试或修改输出。

45 轮耗时最小 2.953 秒、最大 8.297 秒、平均 4.167 秒；15 个独立工作台全部重开持久库一致。统计仅代表此次套餐路由和时段，不是性能承诺。prompt ingest 耗时不计入均值。

## 人工复核

| 场景 | 数量 | 结果 |
| --- | --- | --- |
| 黑白侦探 | 3 组 × 3 轮 | 首轮后锁主体。短发、风衣、右手信保留，改冷侧光和半身近景不改主体层。 |
| 双人帽子 | 3 组 × 3 轮 | 左边不戴帽、右边黑帽保持；只改光线和车站背景；全局 negative 不含 hat。 |
| 木刻交接包裹 | 3 组 × 3 轮 | 两人、左右双手交接、密集排线、逆光保留。扩写补衣物/表情/环境/材质，warnings 逐项说明，无新增镜头角度或构图变化。 |
| 手改英文 | 3 组 × 3 轮 | 实际保存 red scarf 后，改光线、换车站都保留；短发、深蓝外套、水彩和负向不丢失。 |
| 炭笔参考 | 3 组 × 3 轮，另 3 次提取 | style pin 后主体为空，原老年男性未复制；新主体女子抱猫保留。删 LoRA 后纯重编译不带回资源。但首组提取漏单色。 |

所有实际 requirements.style.artists 均为空。三个炭笔末轮要求资源均为空、changed_layers=[]。无真实 GPU、无图片发送、无生成任务；左右动作及风格最终能否成图仍待验证。

## 炭笔提取问题

参考库上传本地 CMA 示例图作为媒体占位，模型仅收到固定测试提示词：

`one elderly man with a beard, charcoal drawing on textured paper, soft smudged shading, monochrome`

这是测试数据，不声称来自图片生成参数，不评价图片与描述是否一致。准备步骤使用生产 ExampleStore / IngestService(source=prompt) / apply_pin 与工作台 CAS，未经过浏览器和 HTTP 路由。

第一组提取保留老年男性、胡须、炭笔、纹理纸及柔和晕染，遗漏 monochrome，并给出不必要的“未确认艺术家标签或资源语法”提示。第二、第三组保留 monochrome。没有在三轮前人工纠正第一组，所以该组正向一直没有单色要求；这是提取遗漏的传播，不是改写在后续丢词。

为检查删除协议，提取后显式添加 `evaluation-charcoal.safetensors` 测试 LoRA 元数据。未安装文件或解析远端资源，只验证声明资源与删除后重编译的状态，不替代真实 LoRA 效果或远端执行验收。

人工判定：改写核心要求重复验证通过；参考提取本样本保真 2/3，未通过完整参考链路验收。后续单独比较提取模型或使用人工确认的参考要求，不能为提取问题反复改变已验证的改写规则，也不能从三次样本推断通用成功率。

## 软件验证与下一步

工具新增 charcoal 场景，单列 planned_prompt_ingests、准备动作、来源哈希、合成资源标识和准备失败停止。新增两项测试验证不发送图片、style pin 不复制主体、删 LoRA 后 stale→重编译、提取失败不进入 rewrite/不泄漏异常。后端全量 427 passed，1 条既有依赖弃用警告。产品 UI 与默认配置未修改。

下一步独立处理提取保真，并准备使用人工确认要求的固定 seed 成图对照。发布 flags 继续关闭；45 轮调用完成不等于产品可以默认发布。
