# 工作台测试迁移（2026-09-10）

原 `WorkbenchPage.test.tsx` 的 29 项用例依赖已经移除的概念输入、生成候选等界面入口，并使用不符合当前 LLM settings / generation targets 响应结构的统一 mock。原文件完整保存在 [LEGACY_WORKBENCH_TESTS.tsx.txt](LEGACY_WORKBENCH_TESTS.tsx.txt)，供历史功能追溯。

迁移后通过 URL 分派模拟响应，16 项用例覆盖当前公开交互：标签去重导入、直出草稿导入、撤销重做、显式 LLM 请求、空负向提示词、失败保留输入且不自动重试、输入改变后结果过期、复制手改结果、工作台保存恢复与修订号、冲突保留编辑、四种远端不可用状态下仍能调整目标，以及恢复并保留历史候选快照。现有 `WorkbenchLlm.test.tsx` 继续覆盖改写模式、设置及实际生成请求字段。

这不是将 29 项旧断言逐一宣称通过：已移除界面的候选操作不再属于当前页面测试范围。没有通过跳过用例或修改测试发现规则隐藏失败；后端原有候选和编译算法测试仍保留。

完整前端套件 67 项通过，TypeScript 检查与生产构建通过。构建沿用 `--emptyOutDir false`，避免当前 Windows 环境清理既有输出目录时的原生进程异常；包体大于 500 kB 的既有提示仍存在。

本批参考库缩略图缓存测试另外覆盖跨实例复用、原图内容/尺寸/条目隔离、损坏缓存重建、容量淘汰不影响其他文件、已删或丢失原图不能从缓存复活，以及 EXIF 旋转。以上均为离线测试，没有调用云 GPU 或收费模型。
