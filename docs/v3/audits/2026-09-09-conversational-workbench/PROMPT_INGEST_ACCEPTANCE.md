# 提示词提取首次验收（2026-09-10）

按用户要求增加 source=prompt 分支，优先使用普通文本模型分析已保存提示词。图片仅留在参考库作人工对照，不读、不传、不消耗视觉输入。读图分析仍可显式选择。

实现与验证：source 参数兼容旧 image 调用；空文本和旧 revision 不调用模型；结果保存 analysis_source；锁层、已确认作者、LoRA、CAS 与失败恢复复用原保护。文本任务关闭思考，首次提取不自动携带光线/构图钉选标记。相关后端 58 项、参考库前端 7 项通过，TypeScript/生产构建通过。

真实调用：OpenCode Go / mimo-v2.5 一次，未发送图片，未自动重试，未使用 GPU。隔离记录在 `.local/conversation-acceptance/prompt-ingest-001/report.json`，没有修改用户参考库。

输入为两个并肩男子，左边不戴帽、右边戴黑帽，木刻、黑墨与象牙色纸、密集平行排线、柔和侧光、半身构图；独立负向是 text / watermark。

结果保留了两个人、左右戴帽关系、黑色、木刻、侧光和半身构图；全局排除只有文字和水印，没有全局禁用帽子；未添加画师或 LoRA，光线与构图随风格钉选标记均为 false。局部不戴帽通过主体文字保留，scoped 数组为空。提示来源为文字、未核对图片。

仍有细节问题：把 parallel hatching 写成了“平行交叉阴影”，混入了交叉排线含义；ivory paper 的中文表达也不够准确。这证明文字提取能减少读图猜测，并不等于不会误译。原始英文仍完整保存在笔记，细节应可人工检查和修正。本次只是单例链路与局部关系验证，不宣称完成多轮保真或成图质量验收。
