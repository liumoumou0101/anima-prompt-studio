# 木刻交接包裹：扩写验收（2026-09-10）

使用新增的 [文字验收工具](TEXT_EVALUATION_RUNNER.md)，从全新工作台顺序执行三轮。配置 OpenCode Go / MiMo-v2.5，启用高级参数，temperature=0.7、top_p=0.9、max_tokens=4096，请求关闭思考。通过生产 ConversationService 与持久化协议，未走浏览器或 HTTP 路由；未发送图片、未调用生图、未更改用户配置。

隔离原始记录：`.local/conversation-acceptance/woodcut-suite-001/report.json`。包含 SYSTEM 哈希、各轮输入/输出、耗时和重开持久库结果。工具报告不自动判定语义通过，本文件记录另外进行的人工审阅。

首轮要求：两个成年人面对面交接同一个包裹，左边双手递出，右边双手接过；黑白木刻、密集平行排线，排除文字和水印。返回保留人数、左右动作与媒介，negative 仅 `text, watermark`，未新增画师或 LoRA。耗时 68.500 秒。

第二轮只增加逆光，changed_layers 只有 lighting；完整正向保留原场景并增加 `backlight`，要求层与负向均未丢失。耗时 34.657 秒。

第三轮 expand 耗时 76.265 秒，数量/动作/媒介/逆光/负向均保留，新增室内环境、专注表情及“中景、平视角度”，并在 warnings 列出三项补充。**人工判定不通过**：泛泛“扩写画面细节”没有明确授权构图变化，模型却更新了 composition，加入 medium shot / eye-level angle。结构合法、warnings 完整不能代替语义合同通过。三轮持久化恢复均一致；失败是扩写边界问题，不是存储失败。

保留原始失败报告和 SYSTEM 哈希，强化 SYSTEM：泛泛扩写不授权镜头、角度、构图或风格改变，原来为空的字段也必须保留空值；同样不能只在英文正向偷加这些内容。环境、纹理、表情的适度补充需逐项提示。该约束属于模型语义规则，不能声称已由程序确定性保证。修订后使用全新输出目录重跑整个三轮场景，不直接续跑第三轮，也不从统计中删除失败组。

## 修订后完整重跑

记录为 `.local/conversation-acceptance/woodcut-suite-002/report.json`，三轮分别 64.047、37.625、57.766 秒，全部完成并重开持久库一致。前两轮保留交接动作和左右归属；第三轮 changed_layers=[]，composition.text/shot 仍为空，正向也不含中景或平视角度。扩写只增加雕刻细节、逆光阴影及专注表情，三项均有 warnings，未新增人物、画师或资源。

本次构图边界专项复核通过；仍有保真备注：中文要求“密集平行排线”保留，但英文仅 `parallel hatching`，缺少密集程度的明确表述。不能据此宣布完整风格保真或模型质量验收通过。negative 从带逗号的 `text, watermark` 变为 `text watermark`，排除含义未变。

两组总计六次真实 rewrite，第一次第三轮的语义失败完整保留。没有自动重试或偷偷修补输出。后端全量 423 passed，1 条现有依赖弃用警告；前端未修改。尚需独立重复、补全五场景套件和后续成图对照。单轮仍耗时约 35～76 秒，稳定性和等待体验不能仅凭此次六次请求均完成而宣布解决。
