# V3 已验证问题与修复顺序

本清单只记录已通过真实远程生成或人工操作复现的问题；它用于约束后续开发方向，避免把不稳定的 AI API 再次放回决定性主路径。

## P0：人工测试被阻断

| 问题 | 已观察行为 | 修复原则 | 状态 |
| --- | --- | --- | --- |
| 自然语言主路径依赖 API 拆解 | 慢、重复结果不一致、字段格式会随机失效 | 本地翻译与本地规则为主；API 仅可选增强 | 首轮已完成 |
| 工作台结果丢失 | 提交远程生成后跳转；返回时解析结果消失 | 原地提交、自动恢复、后续写入工作区库 | 已完成首轮前端恢复 |
| 解析耗时过长 | 单次常见 50–100 秒，配置超时 180 秒 | 主流程不等待 API；API 必须可取消、可超时、可降级 | 主流程已替换 |
| 解析失败即丢失工作 | `pose/action/lighting` 返回数组、枚举越界 | API 不得阻塞主路径；保留原文和上次成功候选 | 首轮已完成 |

## P1：候选不足且不可解释

| 问题 | 已观察行为 | 修复原则 | 状态 |
| --- | --- | --- | --- |
| 画师只有一个自动结果 | 无法比较，也不知道被选择原因 | 展示 Top 10，可见分数、理由、来源；默认不应用 | 首轮已完成 |
| 自动扩展标签漂移 | `snow` 扩展为 `snowman/christmas` 等 | 默认只给建议池，用户选择后再编译 | 首轮已完成；仍需真实数据回归 |
| Hybrid 与 Literal 差异失控 | Literal 可退化为黑白线稿；Hybrid 依赖 API 画面计划 | 建立稳定的本地基础提示词与风格预设 | 待实现 |

## P2：风格与模块完整性

| 问题 | 已观察行为 | 修复原则 | 状态 |
| --- | --- | --- | --- |
| 二次元立绘风格易丢失 | API 提取只保留事实，丢掉线稿、赛璐璐、立绘意图 | 由本地翻译和风格预设保留并显式展示 | 待实现 |
| 画师页面不可打开 | 导航明确标记为未开放，只有推荐接口 | 提供搜索、详情、Top 10 选择池和对照 | 待实现 |
| 小道具/场景细节漂移 | 神乐铃、月牙岩柱等被错误解释 | 可编辑的本地短语与排除词优先于自动扩展 | 待实现 |

## 固定修复路线

本路线受 [PROMPT_CORE_LESSONS_AND_BOUNDARIES.md](PROMPT_CORE_LESSONS_AND_BOUNDARIES.md) 约束。V2 本地翻译是可编辑的语义保底，不是结构权威；V2 `PromptPipeline` 也不整体回接。

1. 已完成首个 `Scene Draft`：原文、译文、已确认标签、待确认建议、未命中内容和风险彼此分离；下一步补充可编辑的实体、动作、关系、构图、风格和排除事实。
2. 已完成翻译 prose baseline：无标签命中也会生成可追踪、可编辑的 Literal，不会返回 422 或擅自补标签。
3. 已完成第一版“确定映射 / 待确认匹配 / 未命中”：原文精确命中或用户选择才进入 Literal；仍需从真实失败族扩展可靠的短语规则。
4. 把 API 提取改成显式的“AI 辅助拆解”按钮，结果作为可审查草案；失败不清空当前工作台。
5. 已完成标签与画师默认不自动写入提示词；下一步让用户选择 1～3 位画师形成明确对照候选。
6. 以固定 Seed、固定 Aesthetic v1.1 工作流重新建立二次元立绘和代表失败族回归集。
