# 2026-09-11 开发交接

## 2026-09-11 GitHub 同步说明

用户要求提交至 GitHub。本次提交统一纳入此前尚未提交的模型版本/种子修复、画廊管线与交互、工作流模板管理、参考案例库、工作台创作流程、画师推荐与本地翻译清理、本地 ComfyUI 接入及测试和审计文档，目标分支为 `feature/llm-workbench`。最新验证为 V3 后端 489 项、前端 113 项和生产构建通过。本地数据库、凭据、测试日志、生成图片及安装包不纳入提交。下文各阶段“未提交/未推送”为当时的历史状态，后续接续以此同步说明及 Git 记录为准。

## 最新续接：本地 ComfyUI 直连（2026-09-11）

已读取上个任务「测试连接」最后讨论，继续完成本地 ComfyUI 环境接入。设置页增加本机地址/端口、连接测试与网页入口，工作流环境页区分本地/SSH；本地执行绕过 SSH，沿用现有生成、恢复、能力预检、资源映射和归档。LoRA 目录和画廊放大也补齐本地连接。保留旧 SSH 快照哈希，修改本机端口会使能力映射失效；修复网页入口复用旧地址与快速检测结果不刷新的竞态。

最终 V3 后端全套 **489 项**、前端全套 **113 项**及生产构建通过；本地连接与维护入口专项 11 项复验通过。真实浏览器在隔离数据库完成新增/保存/测试本地连接、网页入口和资源检测；协议模拟器验证提交、下载和不重复提交的任务恢复，未运行真实 GPU 生图。详情见 [本地 ComfyUI 接入](LOCAL_COMFY_20260911.md)。

材料 `.local/local-comfy-20260911/`；隔离服务 session40692、3368 均通过 stop 标记正常停止，测试页面关闭。原开发服务未动，需要正常重启加载新后端。未调用外部 LLM、未连接云主机、未安装真实 ComfyUI，未提交推送或制作安装包。后续为真实本地/云端环境资源与成图联调，当前协议验收不能冒充硬件/画质验收。

以下为此前阶段的历史记录。

后续已完成：[Aesthetic 1.0/1.1 独立版本选择与真实成图验收](AESTHETIC_VERSION_SPLIT_20260911.md)。当前 V3 后端 465、前端 86 项通过，两个版本各从真实页面生成一张，权重/参数核对正确。用户说明 Turbo v1.1、MiaoMiao 等后加模型也允许调整，不能将它们当作必须保留的管线；仍要求继续处理 V2 遗留问题，不制作安装包。

最新方向与检查：用户明确暂不制作安装包，指出 V2 参数最初用于快速打通功能，要求重点排查继承管线；当前用户观察 Turbo、MiaoMiao 较正常，其他模型历史效果异常。已确认并修复模型切换参数继承及旧候选隐藏提示词；已确认画廊再出图/放大仍有独立旧管线，待统一。详见 [V2 管线专项检查](V2_PIPELINE_AUDIT_20260911.md)。真实页面六张连续生成结果见 [界面延迟测试](UI_LATENCY_TEST_20260911.md)。

本机接续进展：已开始第一轮质量审计和修复，具体修改、作者文件哈希、13 张真实成图、完整 API 验收和仍未解决的边界见 [第一轮审计](QUALITY_AUDIT_20260911.md)。下文“明天”“未修复”为凌晨原始交接状态，最新进展以该报告为准。

用户要求将当前成果和问题推送到 GitHub，明天继续。本次不是正式发布，也没有预约自动执行。

## 最新方向修正：进入全模型生图效果优化阶段

用户后续明确：此前是“先开发功能，再优化效果”，现在已到优化生图效果的阶段。检查范围覆盖所有已支持模型及其基线/实验管线，不限于 Aesthetic。必要时允许重写整个生图管线，不以复用 V2 或维持旧结构为优先目标；保留用户数据及必要的安全、幂等、恢复能力。

**不能再把 V2 当作正确答案。** 用户担心的不是 V3 是否忠实继承 V2，而是 V2 的设计、预设和隐式处理本来就可能导致效果差，V3 又继承了它们。此前“旧新有效节点一致”的检查只证明一致性，不能证明质量或模型适配正确；以旧实现作为测试预期也可能固化错误假设。

必须从模型作者/可信原始来源独立建立参考基线，先直接在 ComfyUI 验证，再与当前软件实际提交图比较。逐项追查旧 presets、模型默认参数、配方回退、自动正负提示词、画师/质量词、LoRA、编码器/VAE/shift、额外模型补丁、二次采样/放大与后处理是否产生非显式影响。以上是待核查风险，不代表已经证明全部存在。

当前优先级从“功能交付收尾”转为“全模型质量基线与延迟优化”。非思考模式、读图仅辅助等既定取舍仍保持；不要用继续修改用户提示词代替管线核验。执行矩阵、证据标准和用户上传对照图见 [全模型质量计划](GENERATION_QUALITY_PHASE_20260911.md)。另一台电脑更新本分支后先读本文和该计划，无需用户再次解释。

## 用户补充（2026-09-11，本机接续）

- 用户提醒：不需要负向提示词只是个别模型的情况，其他大部分模型也需要补充负向提示词。开发时必须按具体模型及版本核验作者说明、负向条件支持、当前传递链路和适合的内容，不能把某模型的例外推广为所有模型负向留空，也不能未经核验给所有模型统一追加负向模板。
- 昨天用户因时间限制，亲自测试的只有美学优化模型（Aesthetic）和快速生图模型（Turbo）。文中其他模型的历史自动测试及视觉复核不代表用户已经亲自验收；其他模型的负向策略和实际效果仍需今天开发时覆盖。
- 问题图“负向为空”是当时实际请求的证据，不是合理默认值的证明，也尚不能单独认定它就是画质差的根因。

## 已完成成果

- 会话工作台重排为对话/画面双栏，提示词、要求、生成设置页签；经典页增加试用入口，低频设置折叠，补齐复制和窄屏交互。
- 修复缺省生成配置与 JSON 字段排序引起的未保存误报；修改非尺寸参数不再误改画幅。
- 官方三图样例随 sdist/wheel/Windows 包携带，界面可显式安装，保留笔记及已有有效包。
- OpenCode 等服务支持刷新模型目录、搜索与下拉选择；真实 OpenCode 返回 36 个模型，不改变当前选择，不调用生成接口。保留已有模型参数和自定义 ID，地址/Key 未保存时禁止刷新。
- alpha.1 完成便携/安装、同版本覆盖安装、卸载和隔离数据保留验证；冻结 EXE 真实生成下载 1 张 Aesthetic 图，同 key 回执重放通过。alpha.2 纳入模型选择修复，重建便携/Setup，新 EXE 模型刷新与数据恢复验证通过。
- 静态基准 v1.1 修正“无合格共现建议仍必须有 conservative lane”的旧预期；required 8/8，泄漏及 validator 错误均为 0。保留旧失败记录，不修改生成算法凑结果。

## 产品取舍，继续保持

采用非思考模式，不要求最强 LLM。提示词提取允许少量可人工修正的细节遗漏；数量、身份、否定等关键语义仍需关注。读图只作可选建议，不是主流程或发布阻塞项。软件层的锁定、保存、幂等、错误恢复必须正确。不要继续反复追加模型试验或堆提示词特判。

## 明天优先处理的两条线

### A. 点击生成到 GPU 开始工作，准备阶段明显过长

用户多次看到本地长时间显示“正在准备 ComfyUI 工作流”，云显卡尚未开始生成，等待有时比生成本身更长。问题是提交前延迟，不是仅缺少进度说明；后来成功不能视为问题消失。

本机观察任务 `f7662c8b-1667-4cc7-a131-75907b845a86` 曾为 preparing，随后变为 running。没有取消或重复提交。独立只读检查：SSH 约 5.3 秒、队列读取约 1.5 秒、2772 个节点的 object_info 约 4 秒。这不是原慢请求的耗时追踪，尚不能解释长等待，也不能宣称已定位根因。

代码中任务准备包含图编译、节点信息获取、输入校验，再提交 `/prompt`；能力检测、维护入口和任务执行分别建隧道，SSH open 还先探测指纹再认证。HTTP 已有超时，但准备全阶段缺乏明确总时限与分阶段耗时证据。

下一步先记录点击/接口接受/目标解析/SSH/环境检查/object_info/参数校验/提交回执的耗时，定位主要等待；然后提前预检、复用健康连接和有效能力缓存、仅重检变化项。保留指纹验证、资源失效处理和结果不明时禁止重复生成。阶段提示是辅助，不能代替降低延迟。

### B. 用户普遍观察到 Aesthetic 效果不如 Turbo

用户强调这是多次人工测试中的普遍现象，不应归结为一张图的措辞或个人偏好。重点检查 Aesthetic 权重版本/身份、编码器、VAE、shift、采样器和配方是否合适，以及是否沿用了不适配的默认配置。

原始小巷学生图已与 run `9c5122ba-03bd-4372-ba31-ec908521f2bc` 逐像素匹配：Aesthetic v1.1，35 步/CFG 4.5/Euler/normal，shift 3，896×1152；用户英文完整送入，负向为空，单阶段生成。旧 22 Aesthetic 与新 v3 Aesthetic 两条记录的有效节点图除 seed/文件名前缀外一致。这只证明两者执行结构相同，不能证明该结构和参数适合 Aesthetic，也不能证明作者推荐工作流同样如此。

最近另有 26_Turbo_v1.1 实验图，启用注意力引导与层重放；应与 23 基线区分。但不能用这个发现否定用户更广泛的 Aesthetic/Turbo 质量反馈。

下一步先核验模型作者来源及参考图定义，在同一 Aesthetic 权重、同提示词/尺寸/seed 下比较作者参考图和当前图；确认 Aesthetic 配置后，再以各自适合的配方比较 Turbo。不要强行共享 CFG/步数当作公平对照，不先承诺重写 V2 兼容层会提升质量。

## 已发现、尚未修复的其他问题

- 会话目标下拉没有清楚展示基线/实验级别和增强节点，尽管后端已提供 experimental/notes。
- 服务器、模型和工作流选择耦合，旧导入图与新模板并列，配置来源不清楚；设置页残留 V2 导入与数据库说明。
- 随机 seed 可达 2^63-1，前端 Number 超过 2^53-1 有精度风险，影响人工输入 seed 复现。服务端冻结重放是另一条路径，不应混为一谈。
- 维护隧道、连接测试、工作流能力、执行目标的状态缺少统一展示。

详细证据见 [工作流与连接分析](audits/2026-09-09-conversational-workbench/WORKFLOW_CONNECTION_ANALYSIS_20260911.md)。这些问题本轮仅分析，不能记为已修复。

## 本机与 Git 边界

代码、合同、验收总结、人工测试清单及上述未解决问题随 Git 同步。`release/` 的 alpha.1/alpha.2 安装包、`.local/` 下原图、脚本、日志、临时数据库不随 Git 同步；用户 `anima-ref/` 参考素材保留本机，没有纳入本次提交。跨电脑继续时不能假定这些本地文件存在。

本机常用入口：项目根 `启动 ANIMA V3.cmd`，运行配置在 `v3/.local/prompt-assistant`；发行包的默认 LLM 配置位置不同。凭据和用户运行时数据库均没有加入 Git。

提交前最新验证：V3 全量 440 项通过，0 失败/错误/跳过；此前 V2 全量、alpha.2 前端 76 项与构建通过，模型刷新后端相关 25 项通过。暂存 diff 检查通过，本地数据和凭据未纳入提交。没有安排明天自动唤醒、没有关闭云主机或取消用户测试任务。


## 续接任务更新：画廊管线与会话工作台（2026-09-11）

此前“拉取 GitHub 最新版本”任务因远程上下文压缩连续失败，已在当前任务续接。本轮隐藏经典工作台入口，画廊再生成接入 V3 持久化提交并锁定原工作流/模型/资源，旧隐式重绘放大改为明确的 1.5× 图像超分；修复上传后 LoadImage 预检误拒绝和画廊任务面板不更新。真实页面分别完成 Aesthetic 1.0、1.1 再生成及一次图像放大。后端 487 项、前端 88 项和生产构建通过。

详细实现、真实 run ID、边界及构建运行时见 [画廊管线改造与验收](GALLERY_PIPELINE_MIGRATION_20260911.md)。原开发服务已重启，隔离验收服务已停止，原图/数据库副本/验证 JSON 留在 `.local/pipeline-20260911`。源码修改仍未提交或推送，不制作安装包。接下来集中到默认参数单一来源、各模型画质配方与增强节点消融；不要把本轮管线正确性验收说成全部画质问题已解决。


## 2026-09-11 12:10：原云显卡 52 张质量测试完成

用户授权“很好，测试吧，还是原来的云显卡”。已完成 42 张基础矩阵（7 权重×3场景×2种子）+10 张单因素，全部通过执行/PNG 参数检查，52 个唯一云端 prompt ID。原 RTX 3080 Ti 收尾 running=0、pending=0，未关闭实例。详细见 `QUALITY_MATRIX_20260911.md`，本机 `.local/quality-matrix-20260911/` 保存全部证据与原图。

主要发现：MiaoMiao 当前配方光影细节较丰富，但仍有暗脸/视线问题；Aesthetic/其他组偏平涂，不是执行串参；Yume 关闭增强仍偏软与简化，NAG+Replay 同开明确复现明显模糊，Miao 同开影响较轻且主要构图变化来自 NAG。Euler→er_sde 没有足够证据统一改默认，Yume 手动通用负向在两例提高清晰度但不解决所有问题。没修改生产默认参数。

基础矩阵实际走 V3 API→提交服务→正式队列→原云端，使用隔离数据库；不声称逐张 UI 点击。完整 Turbo1.0 是隔离参考模板，旧 Base+LoRA 的 Turbo/DMDX 仅做身份审计，不能冒称真实验收。原开发服务保持运行；只读图片报告服务 exec session32278、127.0.0.1:57258 保留供用户查看（仅允许 html/png/jpg）。生图脚本 session33239、26706 均已完成，不要重复运行同一矩阵。

## 2026-09-11：第一轮画廊体验与剩余依赖审计完成

用户讨论产品方向后授权开始。本轮完成画廊 Shift 连选、鼠标框选及追加、单张取消、多选模式、当前结果全选范围说明；回收/恢复/删除保留失败选择，同名改名恢复返回 `restored_trash_paths`。会话工作台结果和画廊共用 `ImagePreview`，支持翻页、缩放、历史参数和关闭；不再默认跳到原图网页。修复预览容器横向偏移和详情抽屉抢占按键。索引仍最多 1000 张，框选仅已渲染图片。

前端全套 93 项通过（maxWorkers=2），画廊新增用例 13 项复验通过；后端画廊/API 59 项通过，生产构建通过。真实浏览器用 12 张隔离副本验证连选、取消、框选、两张回收及恢复、预览信息翻页和 Escape 返回详情。未删除正式图片，未新增云端生图任务。临时服务 session99938 / 60670 已通过 stop 标记正常停止，测试标签已关闭，材料保留在 `.local/gallery-interactions-20260911/`。此前原开发服务和质量报告服务未动；原开发服务须正常重启才能加载新的后端恢复回执字段，前端构建已更新。

审计确认仍有实际 V2 依赖：自然语言适配器、本地 Marian 翻译接线、画廊文件工具；队列旧模块只是 V3 兼容别名。不能宣称已经彻底移除本地翻译模型。复杂工作流自动绑定有明确限制，HIRES_FIX 编译分支仍含特殊参数行为。见 [本轮审计、实现边界与后续顺序](V3_CONSOLIDATION_20260911.md)。下一阶段先工作流/环境/资源映射前台，然后独立参考案例库、工作台创作流程、画师推荐前台和本地 ComfyUI 接口；这些后续项尚未实现。没有提交、推送或打包。

## 2026-09-11：工作流与环境管理离线阶段完成

用户询问是否需要开显卡，答复本地开发不需要，用户授权继续。本轮新增侧栏 `/workflows`：本地模板搜索/模型筛选、文件与节点声明、版本与来源、离线导入/解析预览/手动模型确认、新副本保存、导出和归档版本恢复；执行环境独立页签复用既有映射/检测能力，设置页改为连接管理和跳转入口。切换环境清空临时凭据及映射草稿。新增 catalog/preview API，修复导出清空路径造成来源版本漂移，保留老格式导入。没有重写或改变生成算法。

前端 96 项、后端相关 95 项和构建通过；真实浏览器在无服务器配置的独立数据库中完成模板副本离线保存（9→10）和无环境提示验收。材料 `.local/workflows-20260911/`；临时服务 session25476 / 62722 在验收后停止，测试页关闭。原开发服务和只读质量报告服务未动；原服务必须重启以加载新 API。未请求用户开卡、未调用云端或提交生图、未提交推送或打包。

详见 [工作流模板库与执行环境管理](WORKFLOW_LIBRARY_20260911.md)。下一步可继续离线开发参考案例库；云端资源目录/映射和单张执行联调留待用户开卡。本地 ComfyUI 直连仍未实现，不要把本地模板管理误称为本地推理接口已完成。
# 最新续接：独立参考案例库（2026-09-11）

用户“好，继续”授权继续离线开发。侧栏 /references 独立案例页已完成，画廊详情及工作台结果可存入并直达案例。页面保存/检索图片、提示词、参数、来源和笔记；区分借用创作要求与沿用原生成条件，两者均新建会话且保留旧工作台草稿。后者由服务端 generation_source 保护原工作流引用，复制精确种子、LoRA、参数和提示词；更改模型/目标需明确解除来源。浏览器验收发现并修复原有案例图片 Cookie 路径问题。

验证：V3 后端 477 项、前端 101 项（maxWorkers=2）与生产构建通过；实际浏览器完成图片预览、案例新会话、旧草稿恢复和画廊收藏跳转。没有连接云显卡、调用 LLM 或真实生图。详细边界及测试见 [参考案例库](REFERENCE_LIBRARY_20260911.md)。隔离材料 .local/references-20260911/，服务收尾停止；原开发服务仍需正常重启加载新后端。未提交、推送或制作安装包。

后续优先工作台主创作流程，再做画师推荐前台扩展/Marian 接线移除与资源测量。自定义案例分类/打包、本地 ComfyUI 直连和新云端联调仍未完成，勿混称本轮已验收。

## 最新续接：工作台创作流程第一轮（2026-09-11）

用户授权继续。本轮增加要求/提示词/参数分步状态、保存并编译、最近一次提示词前后对照、初始想法命名、按批次查看结果，以及从所选历史任务直接新建工作台。新接口 POST /api/v3/generation-runs/{run_id}/workspace 接受空 JSON 对象，使用服务端快照，与案例库复用同一创建逻辑，不额外生成案例或出图。原工作台草稿保留；参数修改保存后不触发 LLM，修正前端“一律提示重编译”和测试的错误模拟。空白会话现在也能手动填写要求后直接编译；结果读取失败可单独重试。

前端全套 107 项、后端相关 152 项与生产构建通过。实际浏览器用隔离数据库、模拟 LLM/历史批次完成参数保存、一次性保存编译、文本对照、按历史任务复用以及旧草稿恢复；未连接云端、未调用外部 LLM、未新增 GPU 任务。详情见 [工作台创作流程](WORKBENCH_FLOW_20260911.md)。材料 .local/workbench-flow-20260911/；验收服务收尾停止。原开发服务和质量报告服务未动，需正常重启程序加载新接口。未提交、推送或制作安装包。

下一阶段按原计划为画师推荐扩展、工作台即时切换推荐模式和 Marian 接线清理/资源测量。结果区目前最多 20 个任务、对照仅最近一次本地操作，不宣称已实现完整历史版本/分支系统。

## 最新续接：画师推荐扩展（2026-09-11）

用户继续授权。工作台增加默认收起的可选画师推荐面板：显式读取标签、按需查询、题材贴合/投稿量优先/均衡即时切换，保留选择，取消旧请求，显示命中依据。只有明确加入/移除才修改本地画师要求，不自动改提示词或出图。推荐排序从设置页移到工作台，旧排序 API 保留兼容。V3 翻译工厂固定 BuiltinOfflineEngine，不再探测或加载 Marian；用户模型文件与 V2 旧实现未删除。

后端 480、前端 112 项全套通过并完成生产构建。真实浏览器使用本地正式标签数据验证三模式、选择保留、收起重开及设置入口。完整本地后端启动工作集约 88 MiB，三标签查询后约 89 MiB；50 高频标签压力查询后约 93 MiB，三模式中位查询耗时约 102～118 ms。未加载 torch/transformers/sentencepiece。这是后端测量，不包含桌面壳/浏览器、大画廊或 ComfyUI，也不宣称测得移除旧模型的实际节省量。

详见 [画师推荐与资源测量](ARTIST_EXTENSION_20260911.md)，材料 .local/artist-extension-20260911/（含 stress/memory.json），隔离验收服务收尾停止。原开发服务/质量报告未动，重启程序后加载新翻译工厂。未调用外部 LLM、未连接云主机、未生成图片；未提交推送或打包。下一阶段本地 ComfyUI 环境接入，云端真实联调仍待用户开卡。
