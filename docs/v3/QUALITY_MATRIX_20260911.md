# 2026-09-11 多场景云端质量测试：52 张完成

使用原有 RTX 3080 Ti，经 V3 `/api/v3/direct-prompt/runs` → 提交服务 → 正式生成队列 → 原云端 ComfyUI。测试使用隔离数据库，不将这批结果混入用户画廊。这批是 API 批量测试，不声称逐张点击 UI；此前画廊再生成与放大已有独立 UI 验证。

完成 7 个完整权重 × 3 个场景 × 2 个固定种子，共 42 张。场景为单人持花、双人咖啡馆互动、雨后车站；人物均为成年人。单人 768×1024，其余 1024×768；种子 8798399215689017476 和 2026091102。各模型相同语义正文，各自使用明确记录的配方，因此比较的是当前配方表现，不是控制所有变量的模型排名。无用户 LoRA、NAG 或 LayerReplay。

| 权重 | 步数 / CFG | 采样器 / 调度 | 正向质量前缀 | 负向 |
|---|---|---|---|---|
| Aesthetic 1.1 / 1.0 | 35 / 4.5 | Euler / normal | 无 | 通用质量负向，不含 score |
| Turbo 1.1 / 1.0 | 10 / 1 | er_sde / simple | 无 | 空 |
| Base 1.0 | 35 / 4.5 | er_sde / normal | score_7 | 通用质量负向加 score_1～3 |
| AnimaYume 1.0 Final | 30 / 5.5 | Euler a / normal | 无 | 空；未冒充有已确认的作者固定负向 |
| MiaoMiao 1.6 | 30 / 4.5 | Euler / normal | best quality, score_7, score_9, very aesthetic, ultra detailed | 作者版本质量负向，含 shiny skin |

Turbo 1.0 使用隔离数据库中的完整权重参考图。旧 `02___Turbo_Fast_T2I` 实际为 Base + anima-turbo-lora-v0.2（0.7）+ NAG + LayerReplay；`05__DMDX__4_Step_DMDX_Distill` 为 Base + DMDX LoRA。不能与完整 Turbo 1.0 权重混称为同一条管线。

检查每张 PNG 内嵌 prompt 与运行记录实际工作流完全一致，并逐项核对权重、正负向、seed、步数、CFG、采样器、调度、尺寸、shift=3、编码器和 VAE，以及没有额外增强节点。保留原始 PNG、SHA-256、RGB SHA-256、回执、实际工作流和耗时。耗时包括准备、加载、生成、下载；不能作为纯 GPU 速度排名。

已复核 Aesthetic 1.1 首张与此前独立作者参考图 RGB 逐像素相同。本轮不重跑此前已完成的 10 文件完整哈希核验；来源与固定版本见 QUALITY_AUDIT_20260911.md。

本机产物：`.local/quality-matrix-20260911/`。`plan.json` 完整输入，`verification.json` 自动核对，`review.html` 可筛选原图，`*-sheet.jpg` 各模型六宫格。基础组与单因素结论见下文。

## 基础组完成后的观察

42/42 生图成功，42/42 通过实际图与请求核对。MiaoMiao 编码器使用 `miaomiaoHarem_anima16_txt.safetensors`；其他模型为 `qwen_3_06b_base.safetensors`。此前完整哈希证据显示两者均为 `cd2a512003e2f9f3cd3c32a9c3573f820bb28c940f73c57b1ddaa983d9223eba`，是同内容别名。核对脚本最初漏列 MiaoMiao 名称，补充证据后按各模型准确文件名检查通过；没有因此替换资源或重新生图。

| 当前配方 | 视觉观察 | 具体局限 |
|---|---|---|
| Aesthetic 1.0 / 1.1 | 平涂动画风，两版构图近似但脸型/明暗有差别，主体和空间关系大体正确 | 部分双人图花朵数量不符，咖啡馆背景过度简化；不足以认定某版普遍更好 |
| Base + score_7 | 两张人像与车站布局正常，画风仍偏动画 | 同种子旧图对照仅前缀变化即明显改变脸型；不能认为缺少前缀不影响结果。双人计数随种子波动 |
| Turbo 1.1 | 阴影硬、人物脸部偏亮；快速配方下主体可辨 | 一张人像左上伪签名；双人花数与桥材质偶有偏差 |
| Turbo 1.0 完整权重参考 | 主体、动作、空间布局大体保留，硬边平涂 | 店招伪文字、桥材质表达不稳定；不是旧导入 Turbo 工作流的验收 |
| Yume 1.0 Final 空负向 | 关闭增强时仍有柔化、简化、旧动画风与较浓色彩 | 人像/背景清晰度随种子变动；不能把所有问题归因于 NAG/Replay |
| MiaoMiao 1.6 | 当前配方的发丝、衣褶、光影和车站细节更丰富 | 面部偏暗，双人视线偏桌面，未完全落实对视；不等同于所有题材最优 |

这些是非盲视觉观察。全部提示明确成人与非露骨场景；没有覆盖所有画风、画师组合、长中文会话经 LLM 改写、多 LoRA、极端分辨率、高清二阶段或连续批量压力。不能据此宣布全部 V2 遗留路径质量验收完成。

## 针对性补测结果

基础组后追加 10 张：Yume 与 MiaoMiao 在人物种子 1 上分别只开 NAG、只开 Replay、两者同时开（6 张）；Aesthetic 1.1 在人物/双人种子 2 上只将 Euler 改为 er_sde（2 张）；Yume 在人物/双人种子 2 上只补通用负向（2 张）。保留其他所有输入。增强各组与基线组成 2×2 开关实验，但每模型只有一个场景/种子，不作普遍增益结论。


补测 10/10 完成，10/10 验证通过：PNG 内嵌工作流与实际记录一致；移除实验节点、还原唯一指定变量并忽略保存名前缀后，与各自基线图一致。没有同时偷改步数、CFG、提示词、尺寸或其他节点。

| 仅改变的因素 | 观察 | 结论边界 |
|---|---|---|
| Yume NAG / Replay / 同开 | NAG 改脸型、手和花束；Replay 单开柔化；同开脸、花、背景均明显发糊 | 在本人物种子复现组合增强损害清晰度；不宜视为默认画质提升 |
| MiaoMiao NAG / Replay / 同开 | NAG 改构图、领口、手和花束；Replay 单开较接近基线；同开出现较轻雾化 | 模型依赖明显，不能称 Replay 必然毁图，也不能称组合普遍有效 |
| Aesthetic 1.1 Euler → er_sde | 人物脸型/发型/阴影变化，双人构图和物件也变化 | 两例没有证明总体更好，不据此更换默认采样器 |
| Yume 空负向 → 通用负向 | 两例线条更清楚，人像脸型/花束变化；双人仍简化且花数不足 | 可作为手动对照选项，不冒充作者固定最佳配方，也不是所有质量问题的修复 |

Base 人物种子 1 与同日此前无前缀原图比对，工作流仅正向 `score_7. ` 与保存名前缀不同，脸型明显变化。Yume 人物种子 1 的历史负向对照除负向、保存名前缀外，只有移除了一个无引用的 ResolutionSelector；不把后者当成影响成图的采样步骤。

## 收尾与可查看产物

- 本轮共 52 个成功任务、52 个不同云端 prompt ID：42 基础 + 10 补测。没有重试重复生成；原图全部保存在隔离输出目录。
- 52 张参数/实际节点/PNG 工作流检查全部通过；源文件身份沿用同日固定版本完整哈希记录。
- 52 张提交后准备计时：中位 1.805 秒，范围 1.609～4.391 秒；接收阶段目标解析中位 0.156 秒、最大 2.828 秒。该计时不包含中文会话 LLM 处理，不代表整个 UI 点击链路，也不能用来宣布历史慢连接问题消失。
- 2026-09-11 12:09:31（北京时间）检查原 RTX 3080 Ti：running=0、pending=0；两批测试进程与临时 SSH 隧道已退出，未关闭云实例。
- 这轮新增测试脚本、图像与报告，没有因小样本擅改生产默认配方，没有安装包或 Git 发布。
- 本机原图对照：`.local/quality-matrix-20260911/review.html`；单因素原图：`supplemental-review.html`。只读本机预览服务 `http://127.0.0.1:57258/review.html` 保持运行供查看；离线也可直接打开 HTML。
- 实验计划与证据：`plan.json`、`supplemental-plan.json`、`verification.json`、`supplemental-verification.json`、`encoder-identity.json`、`cloud-final-status.json`、`timing-summary.json`，以及每案例目录的请求、回执、实际节点图、运行记录和 PNG。

本轮足以说明测试到的基础路径能准确执行、组合增强不是普遍收益；尚不足以宣称所有模型画质最佳或所有 V2 遗留图验收完成。尤其旧 Base+蒸馏 LoRA 的 Turbo/DMDX 导入图，不能用本轮完整 Turbo 权重参考结果代替真实验收。
